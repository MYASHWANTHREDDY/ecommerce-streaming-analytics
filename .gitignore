data/dataset.csv
data/*.zip
.venv/
__pycache__/
*.pyc
pgdata/
bronze/
checkpoints/
ivy2-cache/
airflow/logs/
# dbt's own run artifacts/scratch state -- regenerated by every `dbt build`.
dbt/target/
dbt/logs/
dbt/dbt_packages/
dbt/.user.yml
# GX's own scaffold/Data Docs/validation results — all derived from bronze_validation.py,
# which is the single source of truth for expectations. Regenerated on first run.
great_expectations/gx/
.pytest_cache/
*.egg-info/
.env
.DS_Store
venv/

# GCP service-account key for export_to_bigquery.py — never commit this.
secrets/

# Local Streamlit secrets (cloud DB credentials) — Streamlit Community Cloud gets its
# own copy via its UI, this is only for testing the hosted code path locally.
.streamlit/

# Local Claude Code settings/session data — not part of the project
.claude/
